# 现有核心表达不了 / 表达式不干净的技能效果

本文记录 standard/wang 包（王·雷伊、王·盖亚）落地时，**能不别扭地实现**与**不能**的边界。
判定原则来自用户要求："宁可少做也不要 hack"：技能定义照做（id / 类型 / 属性 / 威力 / PP /
先制 / 命中率 / desc 照抄 wiki 原文），够不着的效果就留空或只做能拆出来的那部分，
在这里逐条写清"原文 → 为什么不行 → 核心要补什么"。

- 技能定义：[`packages/seer-core/standard/wang/skills.lua`](../packages/seer-core/standard/wang/skills.lua)
- 效果实现：[`packages/seer-core/standard/wang/effects.lua`](../packages/seer-core/standard/wang/effects.lua)
- 挂载与配点：[`packages/seer-core/standard/wang/init.lua`](../packages/seer-core/standard/wang/init.lua)
- 测试：[`packages/seer-core/test/test_min.lua`](../packages/seer-core/test/test_min.lua)
- 数据来源：biligame 技能页原文 `https://wiki.biligame.com/seer/index.php?title=技能:<ID>&action=raw`
  （抓取与双向核对另见 `docs/skill-research.md`，本包字段以该文交叉核对过的结果为准）

## 2026-09-20 核心补齐后的状态

已增加独立伤害/回复/HP 时机，以及对象持有的 Buff 实例、刷新、到期与分类驱散。API 与生命周期见 [battle-rules.md](battle-rules.md)。**核心可表达，不等于具体技能已经接线。** 本次仅把雷神觉醒迁入 Buff，把惊颤霹雳的 500 固伤拆成独立伤害事件；王系其余技能与旧参考效果保持下面列出的实现范围。最新六只精灵仍是资料，没有自动生成全部技能。

## 〇、本版技能集合（按用户原话「最后学会的四个技能」）

字面读法 = **学习等级最高的四个非第五技能**（原始技能串里 `技能ID-学习等级`，`追加`/`第五` 不占普通四格）：

| 精灵 | 四技能（学习等级最高的四个非第五技能） | 第五技能（Pet.fifth） |
| --- | --- | --- |
| 王·雷伊 | 19733 寰宇雷神断(94) / 24971 传承王意(93) / 19732 惊颤霹雳(92) / 24970 万鸣齐闪(91) | 19734 王·万霆朝宗(95) |
| 王·盖亚 | 30157 威斗天罡破(75) / 25184 不败之境(74) / 30156 极度冲刺(73) / 25183 战霸天下(72) | 30158 王·圣勇战意(76) |

上一版按「3 个本系主技能 + 追加技能」取的 19731 金翼剑轮、30122 王·联盟审判[追加]、
30155 天威力破、30185 王·联盟审判[追加] 已降级为**参考技能**（第四节），效果实现全部保留。

## 一、已干净实现（不在未实现清单里）

| 技能 | ID | 已实现的效果 | 实现位置 |
| --- | --- | --- | --- |
| 传承王意 | 24971 | 全属性+1；自身当前体力低于对手时强化效果翻倍 | `wang_chuancheng_all_stats` |
| 惊颤霹雳 | 19732 | 额外附加 500 点固定伤害（攻击后独立结算，不吃攻击增伤、克制与暴击倍率） | `wang_shock_flat_500` |
| 王·万霆朝宗（第五） | 19734 | 攻击时造成的伤害不会出现微弱（克制倍率 (0,1) 抬回 1） | `wang_fifth_no_weak` |
| 王·万霆朝宗（第五） | 19734 | 未击败对手则自身全属性+1 | `wang_wanting_survive_all_stats` |
| 威斗天罡破 | 30157 | 造成的伤害提升，倍数 = 对手能力下降等级总和 × 10% | `wang_weidou_debuff_boost` |
| 不败之境 | 25184 | 全属性+1；自身当前体力高于最大体力的 1/2 时强化效果翻倍 | `wang_bubai_all_stats` |
| 王·圣勇战意（第五） | 30158 | 攻击时造成的伤害不会出现微弱（与 19734 共用定义） | `wang_fifth_no_weak` |
| 王·圣勇战意（第五） | 30158 | 吸取对手能力提升状态（对手清正等级、自身得同额） | `wang_shengyong_steal_positive` |
| 万鸣齐闪 | 24970 | "每处于一种能力提升状态则伤害提高 10%"的**数学**（定义并单测，暂无载体） | `wang_wanming_stage_boost` |

口径说明：

- **"全属性"= 六项**：攻击 / 防御 / 特攻 / 特防 / 速度 / 命中，正好是 `Pet.STAT_STAGE_FIELDS`。
  交叉旁证：24120 战神狂怒的原文把"全属性"逐项写成这六项。
- **"每处于一种能力提升状态"按项数算，不按等级数**：24970 的 10% 乘的是"有几项能力 >
  0 级"（2 项 = ×1.2），这与 30157 的"能力下降**等级总和**"（-3 与 -1 合起来是 4 级）不是
  一回事。25184 的"高于最大体力的 1/2"是严格大于，恰好一半按 +1。
- **"不出现微弱"不覆盖免疫**：只把 (0,1) 的克制倍率抬回 1；0（无效）保持 0。

## 二、用现有 API 组合的"模拟实现"（语义近似，待核心提供原生入口）

### 1. 极度冲刺（30156）/ 万鸣齐闪（24970）：反转自身能力下降

- 原文：**反转自身能力下降状态**
- 实现（`reverseNegativeStages`）：遍历 `Pet.STAT_STAGE_FIELDS`，对每个负等级发一条
  "增量 = -2×当前等级"的 `room:changeStatStages`（-n → +n）；正等级与 0 不动；没有负等级
  时不产生任何写入。数值结果与反转一致。
- 近似的边界：`core/events/stat_stage.lua` 已经定义了 `operation = "reverse"` 的数据契约
  和 `Before/AfterStatStagesReverse` 时机，但注释明确"本次不提供 Room 反转函数"。
  组合写法走的是普通 `change` 操作、发 `operation = "change"` 的通知，也没有专用的反转
  前后时机；`docs` 与技能 desc 都按"用能力变化组合近似"注明。
- 需要核心补：**`BattleRoom:reverseStatStages(target, source, reason)`**，实现已定义好的
  `reverse` 操作并复用 `Before/AfterStatStagesReverse`；补齐后应把这两条效果迁移过去。
- 结算时机：两条都挂在 `SkillUsed`（命中判定之后、伤害链之前）。30156 是攻击技，
  所以反转出来的正等级会参与本次伤害计算；原文没写先后，这里按"使用技能时先反转"处理。

### 2. 王·圣勇战意（30158）：吸取对手能力提升状态

- 原文：**吸取对手能力提升状态，吸取成功则吸取对手300点体力**
- 实现（`wang_shengyong_steal_positive`，`AfterAttack`）：读对手所有正等级 →
  `room:clearPositiveStatStages`（清正等级走 `clear_positive`，不冒充弱化）→
  `room:changeStatStages` 以同额加给自身（上限仍受 ±6 约束）。
- 边界：只实现了"状态转移"这半句；"吸取 300 体力"尚未接线；现在可用 room:damage + room:recover 组合，但应先确定按实际扣血还是请求量回复。
- 时机说明：放在 `AfterAttack`（命中并结算完伤害之后），符合"命中后吸取"的直觉；
  原文没有写明先后。

## 三、每条技能的"已实现 / 模拟 / 未实现"三态表

| 技能 | ID | 已实现 | 模拟实现 | 未实现 |
| --- | --- | --- | --- | --- |
| 寰宇雷神断 | 19733 | — | — | 50%麻痹 + 按能力提升状态提高概率 |
| 传承王意 | 24971 | 全属性+1、体力低于对手翻倍 | — | 2回合每回合回满血（1/1）；5回合免疫并反弹异常 |
| 惊颤霹雳 | 19732 | +500 固定伤害 | — | — |
| 万鸣齐闪 | 24970 | 每项能力提升 ×10% 的数学（无载体） | 反转自身能力下降 | 直接造成 260 点电系伤害 |
| 王·万霆朝宗 | 19734 | 伤害不微弱；未击败对手则全属性+1 | — | 消回合；消成功则下回合攻击伤害+100%；未击败则下回合先制+2 |
| 威斗天罡破 | 30157 | 按对手负等级总和 ×10% 增伤 | — | 对手能力下降时自身先制+1 |
| 不败之境 | 25184 | 全属性+1、体力 > 1/2 翻倍 | — | 4回合每回合吸对手最大体力 1/3；下2回合先制+2 |
| 极度冲刺 | 30156 | — | 反转自身能力下降 | — |
| 战霸天下 | 25183 | — | — | 免疫反弹异常；免疫能力下降；下次受伤 200% 反伤 |
| 王·圣勇战意 | 30158 | 伤害不微弱；吸取对手能力提升 | — | 吸 300 体力；对手有提升则先制+2 |

## 四、完全未实现（原文 → 为什么不行 → 核心要补什么）

### 1. 寰宇雷神断（19733，物理/电/150/PP5/95%）

- 原文：**50%使对手麻痹，自身每处于一种能力提升状态则概率提高8%**
- 为什么表达不了：核心**没有异常状态系统**。麻痹不是能力等级也不是伤害数值，
  它需要"精灵身上的状态标记 + 出手/命中环节查询 + 回合递减"。按概率触发本身没问题
  （`ctx.logic.rng` 可用），缺的是状态本身。
- 需要核心补：异常/控制状态系统（登记状态类型、挂到 pet、影响出手与命中、按回合递减
  与清除），外加"免疫异常状态"的查询入口。

### 2. 万鸣齐闪（24970）后半段

- 原文：**直接造成260点电系伤害，自身每处于一种能力提升状态则造成的伤害提高10%**
- 当前状态：已有 `room:damage{kind="fixed", damage=..., element=...}`，属性技能也可以主动调用。260 数值和按强化项数增伤尚未接到本技能；需要核对这里的“电系伤害”是否参与特定克制/抗性，不能自动套攻击公式。
- `wang_wanming_stage_boost` 仍只定义和单测。接线时应在专用效果中计算数值，再发独立伤害事件。

### 3. 王·万霆朝宗（19734）的第二、四段（第三段已实现）

- 原文：**消除对手回合类效果，消除成功则下回合自身造成的攻击伤害额外提升100%；
  未击败对手则自身全属性+1；未击败对手则下回合自身所有技能先制+2**
- 已实现的"未击败对手则全属性+1"用 `AfterAttack` + `logic:isFainted(target)` 判定，
  干净可做（见第一节）。其余两段：
  1. 现在可用 `room:dispelBuffs(target,"turn",source)` 返回实际消除数，并用带 start_round 的 Buff 表示下回合增伤；本技能尚未接线，旧 pet 临时字段也尚未迁移到该分类。
  2. "下回合自身所有技能先制+2"：技能先制度是静态字段（`Skill.priority`），而且出手顺序
     在 `GameLogic:doRound` 里**先排序、后触发 `DecidePriority`**，效果时机改不动已定的顺序。
- 仍需补：排序前的先制修正钩子（把 `DecidePriority` 拆成
  "收集修正 → 排序 → 公布"）。

### 4. 不败之境（25184）后两段

- 原文：**4回合内每回合使用技能吸取对手最大体力的1/3；下2回合令自身所有技能先制+2**
- 当前状态：前者已有 Buff + 伤害/回复基础，可在使用技能时结算；仍需定义每回合次数以及按实际扣血还是请求量回复，本技能尚未接线。后者仍缺排序前先制修正（第 3 条第 2 点）。

### 5. 战霸天下（25183）

- 原文：**4回合内免疫并反弹所有受到的异常状态；5回合内免疫能力下降状态；
  将下次受到的伤害200%反馈给对手**
- 当前状态：异常状态种类与影响尚未实现；免疫能力下降现在可由 Buff 监听 BeforeStatStageChange；反伤现在可在 AfterDamage 调用 room:damage，须排除反伤递归。本技能仍是 `effects = {}`，本轮没有擅自补全技能规则。

## 五、参考技能（不占四格，未挂到精灵上）

上一版按「3 个本系主技能 + 追加技能」取到的四个技能，本版不占格、不挂载，
但**定义与效果实现全部保留**，`test_min` 的确定性检查继续用它们：

| 技能 | ID | 状态 | 说明 |
| --- | --- | --- | --- |
| 金翼剑轮 | 19731 | 模拟实现 | 免疫下 1 次受到的攻击（`wang_guard_grant` / `wang_guard_immune_attack`，pet 局内计数 + 一次性效果，`BeforeAttack` 整体取消；`AttackImmune` 通知便于观察） |
| 天威力破 | 30155 | 部分实现 | 伤害不足 280 则下 2 回合必致命（`wang_tianwei_crit_mark` / `wang_tianwei_guaranteed_crit` / `wang_tianwei_crit_expire`）；消回合、免疫异常未实现 |
| 王·联盟审判[追加] | 30185 | 部分实现 | 伤害不足 300 则下 1 次攻击伤害 +100%（`wang_lianmeng_damage_mark` / `wang_lianmeng_damage_double`）；对大暗黑天长老 +50% 未实现 |
| 王·联盟审判[追加] | 30122 | 未实现 | 对大暗黑天长老 +50%（无目标分类钩子）；35% 概率麻痹 2 回合（无异常状态系统） |

### 金翼剑轮（19731）——"免疫下 1 次受到的攻击"的模拟边界

- 计数在 pet 字段上（`wang_immune_attacks`），不参与"效果分类"：**无法被"消除对手回合类
  效果""驱散"之类的机制处理**，也不会随换人/下场统一清理（当前战斗模型没有换人），
  效果之间也无法叠加成多层护盾。
- 需要核心补：原生"免疫次数 / 护盾 / 抵挡"系统（可分类、可清除、可叠加、可过期）。

### 天威力破（30155）——"下 2 回合必致命"

- 状态挂在 pet 上（`wang_crit_guarantee` + 两个常驻 Effect），`TurnEnd` 到时清理；
  "回合"直接读 `logic.round`。"攻击"的定语（是否区分物理/特殊、是否含固定伤害）原文没有
  细化，这里按"下一次造成伤害的攻击"处理；多段攻击只翻第一击。

## 六、核心需要补的能力（汇总）

| 缺口 | 影响的效果 | 建议 |
| --- | --- | --- |
| 异常/控制状态系统（麻痹等）+ 免疫异常查询 | 19733、25183、30122、30155 | pet 上的状态标记 + 回合递减 + 出手/命中查询；配"免疫异常"标记 |
| 回合类效果分类与驱散 | 19734、25184、25183、30155 | 核心已支持对象持有的 Buff 与 room:dispelBuffs；技能和旧临时字段待迁移 |
| 规则层固定/百分比伤害 | 24970、30158、25184 | 已有 room:damage，带来源、类别、前后时机和实际扣血；具体技能待接线 |
| 规则层吸血/回血 | 25184、30158 | 已有 room:recover，可结合 DamageData.actual；具体吸取量、时序仍须按技能定义 |
| 排序前的先制修正钩子 | 30157、19734、25184、30158 | 把 `DecidePriority` 拆成"收集修正 → 排序 → 公布" |
| `BattleRoom:reverseStatStages` | 30156、24970 | 实现已定义的 `reverse` 操作与 `Before/AfterStatStagesReverse`；迁移现有组合近似 |
| 原生免疫次数 / 护盾系统 | 19731（参考，现为模拟） | 可分类、可清除、可叠加、可过期 |
| 目标/阵营分类钩子 | 30122、30185 | 技能与 pet 的 tags 匹配，或图鉴层的分类字段 |
| 第五技能的 PP 账本 | 19734、30158 | 已由 GameLogic 初始化，test_min 已移除手工补账 |
| 多段/连击与一次性标记的语义 | 30185（多段时） | 明确"下一次攻击"是否指整次攻击 |

## 七、数据与口径说明（不编造）

| 项 | 说明 |
| --- | --- |
| 命中率"必中（95% / 100%）" | wiki 与 SeerAPI 的 `must_hit = true` 对应本包 `accuracy = 0`（必中）；19732 / 30157 的 SeerAPI 记 `must_hit = false, accuracy = 95`，取 95。19733 的 wiki 写"必中（95%）"，按 Lead 判定取 95 |
| `-1` 标记含义 | 原始技能串里 24970/24971/25183/25184 等带 `-1`，wiki 未说明其语义；本版按"学习等级最高的四个非第五技能"选取，与 `-1` 无关（见 `docs/skill-research.md` §3） |
| 学习力 / 性格 | 不是图鉴数据。`init.lua` 给两只精灵配了体力+防御学习力与"大胆 / 马虎"性格，只为让演示与自动测试在任何种子下稳定（避免一次致命一击就提前结束剧本），不是官方配点 |
| 效果参数（如 260、300、1/3） | 来自 biligame 原文；部分技能的参数仍未接线（现在已有规则层入口），只记录在案，不编造替代数值 |
