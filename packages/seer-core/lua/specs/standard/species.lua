-- SPDX-License-Identifier: GPL-3.0-or-later
--
-- ============================ standard 包：种族 ============================
--
-- 测试用的标准阵容：雷伊（电系）和盖亚（战斗系）。
-- 目的是给"技能 + 效果 + 印记"当练习场——后续往 skills.lua 里加技能，
-- 用这两只精灵互殴来验证。
--
-- ---------------------------- 数据来源 ----------------------------
--
-- **属性、图鉴编号、种族值都是照图鉴抄的**（来源：赛尔号 WIKI，
-- wiki.biligame.com/seer/雷伊 与 /盖亚，2026-06 抓取）：
--
--   雷伊  序号 70   电系    体力71  攻击108 防御70  特攻101 特防77  速度105  总532
--   盖亚  序号 261  战斗系  体力105 攻击119 防御87  特攻96  特防78  速度105  总590
--
-- 两只都是"攻高防低"的典型：速度同为 105，但雷伊偏特攻、盖亚偏物攻。
--
-- ---------------------------- 重构后的取舍 ----------------------------
--
-- * `ability = "静电庇护"` 这个字段**删掉了**：重构后的 `PetSpecies` 只认
--   name / id / elements / base_stats（见 core/pet.lua），特性（官方叫"魂印"）
--   这套机制还没有着落——旧的写法是"特性 = 挂在时机上的技能"，
--   依赖已经删掉的 `SeerTiming.*` 旧时机名和 `pet:getHpRatio` 之类的 Pet 旧方法
--   （见 git 历史里的 skills.lua）。等特性/触发体系重建后再补。

return {
  name = "standard",

  species = {
    {
      id = 70,
      name = "雷伊",
      elements = { "电" },
      base_stats = {
        hp = 71,
        attack = 108,
        defense = 70,
        sp_attack = 101,
        sp_defense = 77,
        speed = 105,
      },
    },
    {
      id = 261,
      name = "盖亚",
      elements = { "战斗" },
      base_stats = {
        hp = 105,
        attack = 119,
        defense = 87,
        sp_attack = 96,
        sp_defense = 78,
        speed = 105,
      },
    },
  },
}
